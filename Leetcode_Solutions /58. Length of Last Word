class Solution {
    public int lengthOfLastWord(String s) {
        int e=s.length()-1;
   while(e>=0&&s.charAt(e)==' ')
   {
    e--;
   }
   int sa=e;
   while(sa>=0&&s.charAt(sa)!=' ')
   {
    sa--;
   }
   return e-sa;


        
    }
}
